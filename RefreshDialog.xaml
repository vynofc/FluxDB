<Window x:Class="FluxDB.RefreshDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Refresh Options" Height="220" Width="420" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Choose what to refresh:" FontWeight="Bold" Margin="0,0,0,8"/>

        <StackPanel Grid.Row="1" Margin="0,4,0,4">
            <RadioButton x:Name="rbEntire" Content="Entire index (refresh all files in root folder)" Margin="0,4" IsChecked="True"/>
            <RadioButton x:Name="rbView" Content="Current view folder only" Margin="0,4"/>
            <StackPanel Orientation="Horizontal" Margin="0,4">
                <RadioButton x:Name="rbSelect" Content="Specific folder:" VerticalAlignment="Center"/>
                <TextBox x:Name="txtSelectedFolder" Width="220" Margin="8,0,8,0" IsReadOnly="True"/>
                <Button x:Name="btnBrowse" Content="Browse..." Width="80" Click="BtnBrowse_Click" />
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="btnCancel" Content="Cancel" Width="80" Margin="4" Click="BtnCancel_Click"/>
            <Button x:Name="btnOk" Content="OK" Width="80" Margin="4" Click="BtnOk_Click"/>
        </StackPanel>
    </Grid>
</Window>