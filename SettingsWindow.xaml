<Window x:Class="FluxDB.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Height="500" Width="550" WindowStartupLocation="CenterOwner"
        Background="#1e1e1e" Foreground="#cccccc">
    <Window.Resources>
        <SolidColorBrush x:Key="BackgroundBrush" Color="#1e1e1e"/>
        <SolidColorBrush x:Key="SecondaryBackgroundBrush" Color="#252526"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#3c3c3c"/>
        <SolidColorBrush x:Key="ForegroundBrush" Color="#cccccc"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#0078d4"/>
        
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource SecondaryBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource SecondaryBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="CaretBrush" Value="{StaticResource ForegroundBrush}"/>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TabControl Background="Transparent" BorderThickness="0,1,0,0" BorderBrush="{StaticResource BorderBrush}">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
                    <Setter Property="Padding" Value="15,8"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border x:Name="Border" Background="{TemplateBinding Background}" BorderThickness="0,0,0,2" Margin="0,0,10,0">
                                    <ContentPresenter x:Name="ContentSite" ContentSource="Header" Margin="{TemplateBinding Padding}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                        <Setter Property="Foreground" Value="White"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="#2d2d30"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>

            <TabItem Header="General">
                <StackPanel Margin="0,20,0,0">
                    <TextBlock Text="Appearance" FontSize="16" FontWeight="Bold" Margin="0,0,0,15" Foreground="{StaticResource AccentBrush}"/>
                    
                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Current Version:" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" x:Name="txtCurrentVersion" Text="0.0.0" VerticalAlignment="Center" FontWeight="Bold"/>
                    </Grid>

                    <CheckBox x:Name="chkAutoUpdate" Content="Automatically check for updates on startup" Foreground="{StaticResource ForegroundBrush}" Margin="0,0,0,15"/>

                    <Separator Background="{StaticResource BorderBrush}" Margin="0,10,0,20"/>

                    <TextBlock Text="Updates" FontSize="16" FontWeight="Bold" Margin="0,0,0,15" Foreground="{StaticResource AccentBrush}"/>
                    <Border Background="{StaticResource SecondaryBackgroundBrush}" Padding="15" CornerRadius="4">
                        <StackPanel>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock x:Name="txtUpdateStatus" Text="Checking for updates..." FontSize="14"/>
                                    <TextBlock x:Name="txtUpdateDetails" Text="Program started with --noupdate. Check here for new versions." FontSize="11" Opacity="0.6" Margin="0,5,0,0"/>
                                </StackPanel>
                                <Button Grid.Column="1" x:Name="btnDownloadUpdate" Content="Download Update" Style="{StaticResource PrimaryButtonStyle}" Visibility="Collapsed" Click="BtnDownloadUpdate_Click"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </TabItem>
        <TabItem Header="Plugins">
                <StackPanel Margin="0,20,0,0">
                    <TextBlock Text="Installed Plugins" FontSize="16" FontWeight="Bold" Margin="0,0,0,15" Foreground="{StaticResource AccentBrush}"/>
                    
                    <Border Background="{StaticResource SecondaryBackgroundBrush}" Padding="15" CornerRadius="4" Margin="0,0,0,10">
                        <ScrollViewer MaxHeight="200" VerticalScrollBarVisibility="Auto">
                            <ItemsControl x:Name="pluginList">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="0,8" Margin="0,0,0,0">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <CheckBox Grid.Column="0" IsChecked="{Binding IsEnabled}" VerticalAlignment="Center" Margin="0,0,12,0" Checked="PluginCheckBox_Changed" Unchecked="PluginCheckBox_Changed"/>
                                                <StackPanel Grid.Column="1">
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="13" Foreground="White"/>
                                                        <TextBlock Text=" v" Foreground="{StaticResource ForegroundBrush}" Opacity="0.6" FontSize="12"/>
                                                        <TextBlock Text="{Binding Version}" Foreground="{StaticResource ForegroundBrush}" Opacity="0.6" FontSize="12"/>
                                                    </StackPanel>
                                                    <TextBlock Text="{Binding Author}" FontSize="11" Foreground="{StaticResource ForegroundBrush}" Opacity="0.5" Margin="0,2,0,0"/>
                                                    <TextBlock Text="{Binding Description}" FontSize="11" Foreground="{StaticResource ForegroundBrush}" Opacity="0.7" TextTrimming="CharacterEllipsis" Margin="0,2,0,0"/>
                                                </StackPanel>
                                                <TextBlock Grid.Column="2" Text="{Binding StatusText}" FontSize="11" Foreground="{StaticResource ForegroundBrush}" Opacity="0.5" VerticalAlignment="Center" Margin="10,0,0,0"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Border>

                    <TextBlock x:Name="txtNoPlugins" Text="No plugins installed. Place plugin DLLs in the Plugins folder." FontSize="12" Foreground="{StaticResource ForegroundBrush}" Opacity="0.5" Margin="0,0,0,10" Visibility="Collapsed"/>

                    <Button Content="Open Plugins Folder" Style="{StaticResource SecondaryButtonStyle}" Click="BtnOpenPluginsFolder_Click" Margin="0,0,0,10"/>
                </StackPanel>
            </TabItem>
        </TabControl>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Cancel" Width="100" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,10,0" Click="BtnCancel_Click"/>
            <Button Content="Save Settings" Width="120" Style="{StaticResource PrimaryButtonStyle}" Click="BtnSave_Click"/>
        </StackPanel>
    </Grid>
</Window>