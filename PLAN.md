Also große sache: der Log Viewer von FluxDB Soll ersetzt werden, dafür machst du ein einfaches Go Programm in Log Viewer/ was mit --log "pfadzurdatei" funktioniert, dann löschen wir den FluxDB Log Viewer komplett und wenn man dann jetzt F8 drückt um den Log zu öffnen wird (im end Produkt) /components/Log_Viewer.exe --log .\log.txt (die exe ist die gebaute log viewer).
Nutze für den Logger "https://github.com/charmbracelet/log"
